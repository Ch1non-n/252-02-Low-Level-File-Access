# Visible Tests

The visible test script:
- builds with `make`
- runs `./bin/io_optimizer samples/input.txt`
- checks output metrics and file correctness
- fails if `TODO(student)` markers still exist in source files
